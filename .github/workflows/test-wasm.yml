# Runs the tests for Babycat's WebAssembly frontend.
name: test-wasm

defaults:
  run:
    shell: bash

on:
  workflow_dispatch:
  pull_request:
  push:
    tags-ignore:
      - 'v*' # Don't run these tests twice when cutting a new version.

env:
  CARGO_TERM_COLOR: always

jobs:
  test-wasm:
    runs-on: ubuntu-20.04

    steps:

    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
          toolchain: "1.56.0"
          override: true
          target: wasm32-unknown-unknown

    - name: Install Node.js
      uses: actions/setup-node@v2
      with:
        node-version: "14"

    - name: Cache the wasm-pack binary
      id: cache-wasm-pack-binary
      uses: actions/cache@v2
      with:
        path: /usr/share/rust/.cargo/bin/wasm-pack
        key: test-wasm-linux-cache-wasm-pack-0-10-1

    - name: Install wasm-pack (if it is not cached)
      run: cargo install wasm-pack --version=^0.10.1
      if: steps.cache-wasm-pack-binary.outputs.cache-hit != 'true'

    - name: Check out Babycat code from GitHub
      uses: actions/checkout@v2

    - name: Run WebAssembly tests
      run: make test-wasm-nodejs
